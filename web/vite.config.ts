// [Vite 설정]
// TODO: React SWC 플러그인(또는 기본 플러그인)을 적용하세요.
// - 서버 프록시: /api -> VITE_API_BASE_URL 로 프록시
// - 별칭 설정: @ -> src
// - 환경변수 읽기: import.meta.env.VITE_*

// 힌트(찾아 타이핑):
// - import { defineConfig } from "vite"
// - import react from "@vitejs/plugin-react-swc"
// - export default defineConfig({ ... })

