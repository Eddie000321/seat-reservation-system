// [컨트롤러]
// TODO: 요청/응답만 담당하는 컨트롤러 함수를 구현하세요.
// - list(req, res): 쿼리 파라미터로 필터를 받아 예약 목록 반환
// - getById(req, res)
// - create(req, res): body 검증 후 서비스 호출
// - cancel(req, res)
// - listSeats(req, res): 좌석 맵 조회
// - hold(req, res): 좌석 임시점유(타임아웃 포함)

// 컨트롤러는 try/catch 로 서비스 에러를 next(err) 로 넘기세요.
// 응답 형식은 { data, error } 형태를 권장합니다.

